{
  "name": "example",
  "private": true,
  "scripts": {
    "dev:moose": "pnpm -C moose-project run dev",
    "dev:nextjs": "pnpm -C integrations/nextjs run dev",
    "dev:express": "pnpm -C integrations/express run dev",
    "dev:fastify": "pnpm -C integrations/fastify run dev",
    "build:moose": "pnpm -C moose-project run build",
    "seed": "pnpm -C moose-project run seed"
  }
}
